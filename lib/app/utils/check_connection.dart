
import 'package:flutter/material.dart';

abstract class Utility
{
  //Returns true if internet connection is available

  //static Future<bool> isNetworkAvailable()
  //async => await InternetConnectionChecker().hasConnection;
}